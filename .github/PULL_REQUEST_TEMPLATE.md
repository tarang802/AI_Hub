## What does this PR change?

<!-- One or two sentences. Note: page *content* is edited on the live site, not here —
     this repo is for code, styling, and the initial content seed. -->

## Checklist

- [ ] `npm run build --prefix client` passes locally
- [ ] `npm run dev` works for both `server/` and `client/`
- [ ] No secrets committed (`.env`, `members.csv`)
- [ ] No unrelated files changed
