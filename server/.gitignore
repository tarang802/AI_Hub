node_modules/
.env
*.log

# Never commit the real member roster — it's personal data (names + college
# emails) and this repo is public. Import it locally with `npm run import-members`.
members.csv
members-provisioned.csv
